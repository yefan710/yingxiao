# 素材

把真实产品截图、功能界面、图标、客户证明、数据证明、服务保障截图放在这里。

不要用 AI 生成假后台、假数据、假客户案例。

## Image2 Base Rule

- `image2_text_bases/` 存放 image2 生成的完整视觉和中文文案排版底稿。
- `screenshots/` 存放后续贴入 mask 的真实产品截图。
- 合成阶段只能把 `screenshots/` 里的真实截图贴到 `image2_text_bases/` 的空白截图槽里，不能重排文案或重画卡片。
